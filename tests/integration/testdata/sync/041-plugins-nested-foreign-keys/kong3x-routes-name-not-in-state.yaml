_format_version: "3.0"
_info:
  select_tags:
    - "managed-by-test"
services:
- name: example-service
  port: 3200
  protocol: http
  host: localhost
  plugins:
    - config:
        minute: 100
        policy: local
      enabled: true
      name: rate-limiting
      protocols:
        - http
    - config:
        minute: 200
        policy: local
      route: external-route # using name instead of UUID - should fail
      enabled: true
      name: rate-limiting
      protocols:
        - http
  routes:
  - name: example-route-1
    paths:
    - ~/r1

