plugins:
- name: openid-connect
  config:
    auth_methods:
    - authorization_code
    - session
    issuer: http://example.org
    client_id:
    - "<client-id>"
    client_secret:
    - "<client-secret>"
    session_secret: "<session-secret>"
    response_mode: form_post